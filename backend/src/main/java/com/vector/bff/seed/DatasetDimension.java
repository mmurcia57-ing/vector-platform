package com.vector.bff.seed;

/**
 * Fixture categories used only by the deterministic seed harness. They are not
 * canonical entity implementations or a persistence schema.
 */
public enum DatasetDimension {
	AREA_DOMAINS,
	SERVICES,
	CONFIGURATION_ITEMS,
	MONITORING_EVENTS,
	INCIDENTS,
	PROBLEMS,
	CHANGES,
	DEPLOYMENTS,
	SLOS,
	SLO_OBSERVATIONS,
	METRIC_OBSERVATIONS,
	EVIDENCE,
	RISK_FINDINGS,
	COMMITMENTS,
	IMPROVEMENT_ACTIONS,
	OUTCOME_VERIFICATIONS
}
